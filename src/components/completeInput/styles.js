import { StyleSheet } from 'react-native';
import { collors } from '../../styles';

export default StyleSheet.create({
  campo: {
    height: 31,
    paddingTop: 5,
    borderWidth: 2,
    borderColor: '#A050BE',
    textAlign: 'center'
  }
});
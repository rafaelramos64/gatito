import { StyleSheet } from "react-native";
import { collors } from "../../styles";

export default StyleSheet.create({
  adjustScreen: {
    flex: 1,
    backgroundColor: collors.purple,
  },
  adjustBottomScreen: {
    flex: 0,
    backgroundColor: collors.orange
  }
})